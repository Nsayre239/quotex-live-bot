services:
  - type: web
    name: quotex-live-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        value: your_token_here
      - key: CHAT_ID
        value: your_chat_id_here
